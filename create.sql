CREATE TABLE store_sales (
    order_id VARCHAR(10),
    product_code VARCHAR(10),
    product_name TEXT,
    quantity INT,
    order_date DATETIME,
    unit_price FLOAT,
    amount FLOAT,
    customer_code VARCHAR(10),
    region VARCHAR(50)
);

